query {
    organisation(id: "") {
        connections {
            publications(first: 100, after: "") {
                pageInfo {
                    startCursor
                    endCursor
                    hasPreviousPage
                    hasNextPage
                }
                edges {
                    node {
                        id
                    }
                }
            }
        }
    }
}
